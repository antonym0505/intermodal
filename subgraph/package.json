{
    "name": "intermodal-subgraph",
    "license": "UNLICENSED",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ intermodal-subgraph",
        "create-local": "graph create --node http://localhost:8020/ intermodal-subgraph",
        "remove-local": "graph remove --node http://localhost:8020/ intermodal-subgraph",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 intermodal-subgraph"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.68.0",
        "@graphprotocol/graph-ts": "0.32.0"
    }
}